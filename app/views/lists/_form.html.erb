<%= simple_form_for(list, wrapper_html: { class: "form-group"}) do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to list", root_path, class: "btn btn-outline-secondary" %>
